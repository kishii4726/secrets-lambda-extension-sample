# secrets-lambda-extension-sample

## Usage

```
$ aws ssm put-parameter --name "SAMPLE_PARAMETER" --type "SecureString" --value "Sample parameters for secrets-lambda-extension-sample
$ sls deploy
```
